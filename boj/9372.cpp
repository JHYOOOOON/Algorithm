#include <iostream>
#include <vector>
using namespace std;

int main()
{
    int T;
    cin >> T;

    while (T--)
    {
        int N, M, a, b;
        cin >> N >> M;
        while (M--)
        {
            cin >> a >> b;
        }
        cout << N - 1 << "\n";
    }

    return 0;
}